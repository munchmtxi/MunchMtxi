const path = require('path');

module.exports = {
  'config': path.resolve(__dirname, '../src/config/config.js'), // Ensure config.js is correctly located
  'models-path': path.resolve(__dirname, '../src/models'),      // Models inside `src/`
  'migrations-path': path.resolve(__dirname, '../migrations'),  // Migrations outside `src/`
  'seeders-path': path.resolve(__dirname, '../seeders')         // Seeders outside `src/`
};
